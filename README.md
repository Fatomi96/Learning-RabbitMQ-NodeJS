# Learning-RabbitMQ-NodeJS
Using a message broker (RabbitMQ) to exchange/communicate between two services in NodeJS environment

# First Run 
Run senderOne against receiver 

# Second Run
Run sender Two against broker
